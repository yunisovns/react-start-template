import type { Meta } from '@storybook/react';
import AddOperationPage from './AddOperationPage';

const meta: Meta<typeof AddOperationPage> = {
  title: 'shared/AddOperationPage',
  component: AddOperationPage,
  args: {},
};

export default meta;

export const Default = {};
