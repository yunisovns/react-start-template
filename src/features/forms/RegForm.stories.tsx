import type { Meta } from '@storybook/react';
import RegForm from './RegForm';

const meta: Meta<typeof RegForm> = {
  title: 'shared/RegForm',
  component: RegForm,
  args: {},
};

export default meta;

export const Default = {};
