import { Meta } from '@storybook/react';
import ModalInputButton from './ModalInputButton';

const meta: Meta<typeof ModalInputButton> = {
  title: 'shared/ModalInputButton',
  component: ModalInputButton,
};

export default meta;

export const Default = {};
